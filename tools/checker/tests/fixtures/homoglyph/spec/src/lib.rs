use patterns::because;

pub const LOAN_DAYS: u32 = 21;
because!(LOAN_DAYS, "how long a member keeps a book before it falls due");
pub const LOAN_DАYS: u32 = 5;
because!(LOAN_DАYS, "a second term that reads as the first to a person and not to a checker");
