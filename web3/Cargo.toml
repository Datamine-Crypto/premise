[package]
name = "premise_web3"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true

description = "the chain library of Premise: account addresses with their checksum, keccak digests, and the decoding of event logs by a declared layout"

[dependencies]
premise = { path = "../patterns", version = "0.1.0" }
premise_macros = { path = "../patterns_macros", version = "0.1.0" }
hex = "0.4"
sha3 = "0.10"
ruint = { version = "1", default-features = false, features = ["std"] }
